@using SPTarkov.Server.Core.Models.Eft.Common
@using SPTarkov.Server.Core.Models.Eft.ItemEvent
@using SPTarkov.Server.Core.Models.Enums
@using SuntionCore.SPTExtensions.Services
@using Color = MudBlazor.Color
@inject I18NMgr I18NMgr
@inject ISnackbar Snackbar
@inject ModConfig ModConfig
@inject DataGetterService DataGetter
@inject ModMailService ModMailService
@inject WebDataContext WebDataContext

<MudPaper Outlined="true" MaxHeight="300px" Class="overflow-y-auto">
    <MudTooltip Text="@("webUI.claimCompensationDesc".Translate(I18NMgr.I18N!))">
        <MudText Typo="Typo.h6">@("webUI.claimCompensation".Translate(I18NMgr.I18N!))</MudText>
    </MudTooltip>
    
    <MudStack Row="true" AlignItems="AlignItems.Center">
        <MudText Typo="Typo.body2">@("webUI.enterAmountNeedReceive".Translate(I18NMgr.I18N!))</MudText>
        <MudNumericField @bind-Value="GetRub" Label="rub" Variant="Variant.Filled" Min="0.0" Max="@long.MaxValue"/>
    </MudStack>

    <MudStack Row="true" AlignItems="AlignItems.Center">
        <MudText Typo="Typo.body2">@("webUI.enterAmountWishConsume".Translate(I18NMgr.I18N!))</MudText>
        <MudNumericField @bind-Value="RemoveRub" Label="rub" Variant="Variant.Filled" Min="0.0" Max="@long.MaxValue"/>
    </MudStack>

    <MudButton Variant="Variant.Filled" Color="Color.Error" StartIcon="@Icons.Material.Filled.Commit"
               OnClick="@(() => ConfirmClaimCompensation())">@("confirm".Translate(I18NMgr.I18N!))
    </MudButton>
</MudPaper>

@code {
    double GetRub { get; set; }
    double RemoveRub { get; set; }

    void ConfirmClaimCompensation()
    {
        double delta = GetRub - RemoveRub;
        double amount = Math.Abs(delta);
        var message = "";
        List<Warning>? warnings = null;
        PmcData? pmcData = DataGetter.GetPmcData(WebDataContext.CurrAccount);
        if (pmcData == null)
        {
            ModConfig.Warn("理赔失败: 玩家数据获取失败");
            return;
        }
        if (amount > Constants.Epsilon)
        {
            if (delta >= 0)
            {
                message = $"{"webUI.receivedThroughClaims".Translate(I18NMgr.I18N!)}: {amount}rub";
                warnings = ModMailService.SendMoney(WebDataContext.CurrAccount, Money.ROUBLES,
                    message
                    , amount);
                RaidRecordMod.Logger.Info($"[WebUI.理赔] 为账户{WebDataContext.CurrAccount}理赔卢布 {amount:F3}(此处为Double类型) 个: {(warnings?.Count > 0 ? "失败" : "成功")}");
            }
            else
            {
                message = $"{"webUI.consumedThroughClaims".Translate(I18NMgr.I18N!)}: {amount}rub";
                var amountLong = Convert.ToInt64(amount);
                RaidRecordMod.Logger.Info($"[WebUI.理赔] 为账户{WebDataContext.CurrAccount}消耗卢布 {amountLong}(此处为Int64类型) 个: {(warnings?.Count > 0 ? "失败" : "成功")}");
                warnings = ModMailService.Payment(WebDataContext.CurrAccount, Money.ROUBLES, amountLong, pmcData);
            }
        }
        
        Snackbar.Add(message);

        if (warnings?.Count > 0)
        {
            ModConfig.Warn(string.Join(", ", warnings));
        }

        GetRub = 0;
        RemoveRub = 0;
    }
}